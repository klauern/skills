!/.claude
# Ignore local settings (user-specific paths)
.claude/*.local.json

!.beads/
.beads/sync-state.json
.beads/daemon.pid

# Dolt runtime files (created by dolt sql-server in repo root)
.dolt/
.doltcfg/
beads_ks/
config.yaml

# Dolt database files (added by bd init)
*.db
.env
.worktrees/
