---
allowed-tools: Glob, Grep, Read, Write, Bash, AskUserQuestion
description: Interactive wizard to generate rules (alias for /generate-rules)
---

## Instructions

This is the tool-agnostic name for the rule generation wizard.

1. Read `plugins/dev-utilities/commands/generate-rules.md`
2. Follow those instructions exactly

**Note**: This command provides the same functionality as `/generate-rules` and `/generate-claude-md`.
